# Sales API Express.js

## Usage

```
npm install
```

Buat file `.env` di root project yang berisi:

```
PORT=<nomor-port>
TOKEN_KEY=>token-key>
```

Import SQL (mariadb) dan jalankan dev server:

```
npm run dev
```
